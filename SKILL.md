---
name: claude-code-engineering-auto
description: Coordinate Claude Code engineering work with project-aware workflows. Use when the user asks for codebase understanding, bug fixing, new feature development, test-first changes, frontend screenshot iteration, refactoring, architecture analysis, CLAUDE.md generation, project rule consolidation, commit summaries, PR descriptions, or any software engineering task that should first inspect project rules, classify the task, analyze files, propose a plan, modify code when appropriate, verify results, and summarize risks.
---

# Agent 使用说明

使用这个 Skill 时，先依据用户工程任务判断类型，再按下方原始流程执行项目规则读取、代码理解、方案分析、修改验证和总结交付。

1. 进入项目后优先检查 `CLAUDE.md`，存在时按项目规则执行。
2. 没有 `CLAUDE.md` 时，先应用默认工程协作规则，并在适合时建议生成或补充项目规则。
3. 修改代码前，先定位相关文件、调用链、现有测试和风险点。
4. Bug、重构、权限、数据、部署等高风险任务，先给方案和风险，再等待用户确认。
5. 修改后运行相关测试、lint、构建或前端截图验证，并汇报验证结果。
6. 任务结束时总结修改文件、验证结果、风险点和可沉淀到 `CLAUDE.md` 的规则。
# Claude Code 工程协作总控 Skill

## 目标

让 Claude Code 在真实工程项目中，自动按照稳定的工程协作流程工作。

用户不需要每次写复杂 Prompt。用户只需要提出一个简单任务，Skill 需要自动识别任务类型，读取项目规则，选择合适流程，然后按工程规范推进。

这个 Skill 的核心目标：

1. 用户用一句话描述任务即可开始。
2. Skill 自动判断任务属于代码理解、bug 修复、新功能开发、测试优先、前端截图迭代、重构分析、项目规则沉淀、提交 PR 等哪一类。
3. Skill 自动检查项目里是否存在 `CLAUDE.md`。
4. 如果存在 `CLAUDE.md`，优先按项目规则执行。
5. 如果不存在 `CLAUDE.md`，先应用默认工程协作规则，并在适合时生成或建议生成 `CLAUDE.md`。
6. 修改代码前，先理解项目、定位相关文件、分析方案和风险。
7. 修改完成后，必须验证、总结、复盘。
8. 对高风险操作，必须先征求用户确认。

## 这个 Skill 的使用方式

用户侧只需要这样提问：

```text
帮我修复登录后白屏的问题。
```

```text
帮我给桌面宠物项目加一个记忆系统。
```

```text
帮我理解这个项目的代码结构。
```

```text
帮我根据这张截图实现页面。
```

```text
帮我重构聊天模块。
```

```text
帮我生成这个项目的 CLAUDE.md。
```

Skill 需要自动把这些简单任务转换成对应的工程流程。

## 总体工作原则

1. 先理解项目，再处理任务。
2. 先判断任务类型，再选择流程。
3. 先读取项目规则，再执行修改。
4. 先分析根因或方案，再改代码。
5. 先验证结果，再总结交付。
6. 高风险操作必须先确认。
7. 能用项目真实工具验证，就不要只靠猜测。
8. 每次任务完成后，尽量沉淀可复用经验。

## 规则优先级

当不同规则之间出现冲突时，按以下优先级执行：

1. 用户当前明确指令
2. 项目根目录的 `CLAUDE.md`
3. 项目本地规则文件，例如 `CLAUDE.local.md`
4. 当前 Skill 的默认规则
5. Claude Code 的通用能力判断

如果用户当前指令与项目规则冲突，需要先说明冲突点，再请求用户确认。

## 自动触发逻辑

当用户提出任何工程相关任务时，无论用户是否写出完整 Prompt，都必须自动识别任务类型，并套用对应工作流。

用户可能会这样说：

```text
帮我修这个 bug。
```

```text
这个项目跑不起来，帮我看看。
```

```text
帮我加一个登录功能。
```

```text
帮我优化这个页面。
```

```text
帮我看下这个函数是干嘛的。
```

```text
帮我把这个模块重构一下。
```

Skill 需要自动完成以下动作：

1. 判断任务类型。
2. 检查是否存在 `CLAUDE.md`。
3. 读取项目结构、配置文件、README、测试目录和相关代码。
4. 选择合适的工作流。
5. 在修改前输出分析和计划。
6. 根据风险程度决定是否需要用户确认。
7. 执行修改、验证和总结。
8. 在任务结束时提出可沉淀到 `CLAUDE.md` 的规则。

## 任务类型识别

### 1. 代码库理解任务

触发关键词示例：

```text
帮我看懂这个项目
```

```text
这个文件是做什么的
```

```text
这个函数怎么被调用
```

```text
这个模块为什么这样设计
```

自动选择流程：

1. 扫描项目结构。
2. 识别技术栈和核心入口。
3. 搜索相关文件、函数、类、调用链。
4. 查看测试文件和文档。
5. 必要时查看 git 历史、issue、PR。
6. 输出项目地图、调用链、关键模块和风险点。

### 2. Bug 根因定位任务

触发关键词示例：

```text
帮我修复这个 bug
```

```text
报错了
```

```text
白屏了
```

```text
测试失败
```

```text
功能不正常
```

自动选择流程：

1. 收集错误现象。
2. 找到相关日志、报错栈、测试失败信息。
3. 搜索相关代码和调用链。
4. 尝试复现问题。
5. 定位根因并列出证据。
6. 提出 2 到 3 个修复方案。
7. 根据风险决定是否等待用户确认。
8. 修改代码并运行测试。
9. 输出验证结果和风险点。

### 3. 新功能开发任务

触发关键词示例：

```text
帮我加一个功能
```

```text
实现一个记忆系统
```

```text
新增用户设置
```

```text
增加登录功能
```

自动选择流程：

1. 理解功能目标。
2. 扫描现有架构。
3. 找到合适的扩展位置。
4. 分析数据流、状态管理、接口和存储方式。
5. 提出 2 到 3 个实现方案。
6. 推荐 MVP 方案。
7. 需要用户确认时先暂停。
8. 实现功能、补测试、运行验证。
9. 输出修改摘要和后续扩展建议。

### 4. 测试优先任务

触发关键词示例：

```text
先补测试
```

```text
这个逻辑需要更稳
```

```text
帮我覆盖边界情况
```

```text
修复这个问题并加测试
```

自动选择流程：

1. 找到现有测试。
2. 分析测试覆盖缺口。
3. 设计失败测试。
4. 先写测试。
5. 再修改实现。
6. 运行测试并迭代。
7. 输出新增测试覆盖的场景。

### 5. 前端截图迭代任务

触发关键词示例：

```text
根据截图实现页面
```

```text
还原这个设计稿
```

```text
帮我修 UI
```

```text
页面样式不对
```

自动选择流程：

1. 分析截图或设计稿。
2. 找到前端入口、组件和样式文件。
3. 实现或修改页面。
4. 启动项目。
5. 使用浏览器自动化工具截图。
6. 对比目标图和实际截图。
7. 继续调整直到接近目标。
8. 输出最终差异和可继续优化点。

### 6. 重构和架构分析任务

触发关键词示例：

```text
帮我重构这个模块
```

```text
这个代码太乱
```

```text
帮我优化架构
```

```text
这个模块怎么拆比较好
```

自动选择流程：

1. 先理解当前模块职责。
2. 分析调用链、依赖、测试覆盖和风险。
3. 从最小改动、长期架构、测试风险三个方向给出方案。
4. 做成本、收益、风险对比。
5. 推荐方案。
6. 用户确认后再执行。
7. 分阶段修改并验证。

### 7. CLAUDE.md 沉淀任务

触发关键词示例：

```text
帮我生成 CLAUDE.md
```

```text
帮我整理项目规则
```

```text
把项目命令沉淀下来
```

```text
以后让 Claude 按这个项目规则工作
```

自动选择流程：

1. 扫描项目结构。
2. 读取 README、package.json、配置文件、测试目录、脚本目录。
3. 提炼技术栈、启动命令、测试命令、构建命令、目录结构和提交规则。
4. 识别高风险操作。
5. 生成 `CLAUDE.md` 草案。
6. 说明哪些规则来自项目文件，哪些是建议。
7. 经用户确认后写入项目。

### 8. 提交和 PR 任务

触发关键词示例：

```text
帮我提交代码
```

```text
帮我写 PR 描述
```

```text
帮我总结这次 diff
```

```text
帮我创建 PR
```

自动选择流程：

1. 读取 git status 和 git diff。
2. 总结修改文件和修改目的。
3. 判断变更类型。
4. 运行必要测试。
5. 生成 commit message。
6. 生成 PR description。
7. 写清楚背景、修改点、验证结果和风险。
8. 推送或创建 PR 前需要用户确认。

## CLAUDE.md 自动检查流程

每次进入项目执行工程任务时，优先检查项目根目录是否存在 `CLAUDE.md`。

### 如果存在 CLAUDE.md

执行规则：

1. 先读取 `CLAUDE.md`。
2. 按其中的启动、测试、构建、提交、风险操作规则执行。
3. 如果任务与 `CLAUDE.md` 冲突，先说明冲突点。
4. 如果发现 `CLAUDE.md` 信息过时，需要指出。
5. 如果任务中发现新的稳定项目经验，在任务结束时建议追加到 `CLAUDE.md`。

### 如果不存在 CLAUDE.md

执行规则：

1. 不要求用户先写长 Prompt。
2. 先扫描项目结构和关键配置文件。
3. 在当前任务中临时应用默认工程规则。
4. 如果任务较复杂，先生成一份简短的项目规则草案。
5. 任务结束时建议用户生成完整 `CLAUDE.md`。
6. 如果用户要求，可以直接生成并写入 `CLAUDE.md`。

## 默认工程规则

当项目没有 `CLAUDE.md` 时，使用以下默认规则。

### 修改前规则

1. 修改代码前先定位相关文件。
2. Bug 任务先找根因和证据。
3. 新功能任务先给实现方案。
4. 重构任务先做风险评估。
5. 前端任务优先截图验证。
6. 测试相关任务优先查现有测试。
7. 不确定项目命令时，先查看 README、package.json 或帮助命令。

### 修改中规则

1. 尽量小步修改。
2. 不做无关重构。
3. 不改和任务无关的格式。
4. 不随意安装依赖。
5. 不随意删除文件。
6. 不改动敏感模块，除非任务明确要求。
7. 对不确定的地方写明假设。

### 修改后规则

1. 运行相关测试。
2. 运行 lint 或类型检查。
3. 前端任务尽量截图验证。
4. 总结修改文件。
5. 总结验证结果。
6. 总结风险点。
7. 给出后续建议。
8. 判断是否有内容值得沉淀到 `CLAUDE.md`。

## 高风险操作确认规则

以下操作必须先征求用户确认：

1. 删除文件或目录。
2. 大规模重命名。
3. 安装新依赖。
4. 修改数据库迁移。
5. 修改认证、权限、支付、安全相关逻辑。
6. 修改生产环境配置。
7. 修改环境变量。
8. 推送代码。
9. 创建 release。
10. 部署线上环境。
11. 执行可能破坏数据的 shell 命令。
12. 清空缓存、清空数据库、删除存储桶内容。
13. 改动范围超过当前任务明显需要的文件。

## 默认响应策略

当用户只给一句简单任务时，不要要求用户补写完整 Prompt。应先做低风险探索。

### 简单任务

如果任务风险低，可以直接执行探索和分析。

示例：

```text
帮我看下这个函数是干嘛的。
```

处理方式：

1. 搜索函数定义和调用。
2. 阅读相关文件。
3. 输出解释。
4. 不修改代码。

### 中等任务

如果任务涉及代码修改，但风险可控，先输出简短计划。

示例：

```text
帮我修复这个样式问题。
```

处理方式：

1. 找相关组件和样式。
2. 给出简短修改计划。
3. 修改。
4. 截图或运行检查。
5. 输出结果。

### 高风险任务

如果任务涉及核心逻辑、数据、权限、支付、部署、重构，必须先输出方案并等待确认。

示例：

```text
帮我重构用户权限模块。
```

处理方式：

1. 理解当前模块。
2. 分析风险。
3. 给出方案。
4. 等用户确认。
5. 分阶段执行。

## 输出格式规范

### 任务开始时

如果需要先说明计划，使用以下格式：

```text
我会先做三件事：

1. 检查项目规则和相关文件。
2. 定位任务涉及的模块。
3. 给出方案和风险，再决定是否修改代码。
```

### 修改前

```text
当前发现：

1. 相关文件：
2. 当前逻辑：
3. 可能根因：
4. 可选方案：
5. 推荐方案：
6. 需要确认：
```

### 修改后

```text
已完成：

1. 修改文件：
2. 修改内容：
3. 验证命令：
4. 验证结果：
5. 风险点：
6. 后续建议：
7. 可沉淀到 CLAUDE.md 的规则：
```

## 自动生成 CLAUDE.md 的模板

当用户要求生成项目规则，或者项目没有规则文件且任务明显适合沉淀时，可使用以下模板。

```markdown
# CLAUDE.md

## 项目简介

描述项目用途、主要技术栈和核心模块。

## 技术栈

列出前端、后端、桌面端、数据库、构建工具、测试框架等。

## 项目结构

说明关键目录和文件的作用。

## 常用命令

### 安装依赖

填写安装依赖命令。

### 本地启动

填写启动命令。

### 测试

填写测试命令。

### Lint

填写 lint 命令。

### 构建

填写构建命令。

## 默认工程流程

1. 修改代码前先理解相关文件和调用链。
2. Bug 修复先定位根因。
3. 新功能开发先给实现方案。
4. 重构任务先做风险评估。
5. 前端任务优先截图验证。
6. 修改完成后输出修改文件、验证结果和风险点。

## 高风险操作

以下操作必须先征求用户确认：

1. 删除文件。
2. 安装新依赖。
3. 修改数据库迁移。
4. 修改认证、权限、支付相关逻辑。
5. 推送代码。
6. 部署线上环境。

## 提交规范

说明 commit message、分支命名、PR 描述要求。

## 团队工具

说明内部 CLI、MCP server、日志系统、部署脚本等工具的用法。

## 备注

记录项目特殊约定和已知坑。
```

## 常用短指令示例

用户可以使用以下短指令，不需要写复杂 Prompt。

### 代码理解

```text
帮我看懂这个项目。
```

```text
这个文件是干嘛的？
```

```text
这个函数在哪些地方被调用？
```

### Bug 修复

```text
帮我修复这个 bug。
```

```text
登录后白屏，帮我排查一下。
```

```text
测试失败了，帮我看根因。
```

### 新功能

```text
帮我加一个聊天记忆功能。
```

```text
帮我实现用户设置页面。
```

```text
帮我加一个导出功能。
```

### 前端 UI

```text
帮我根据截图实现页面。
```

```text
这个页面和设计稿不一致，帮我调整。
```

```text
帮我用截图验证 UI。
```

### 重构

```text
帮我重构这个模块。
```

```text
这个文件太大了，帮我拆一下。
```

```text
帮我分析这个模块怎么优化。
```

### 项目规则

```text
帮我生成 CLAUDE.md。
```

```text
把这个项目的规则沉淀一下。
```

```text
以后让 Claude 按这个项目规则工作。
```

### 提交和 PR

```text
帮我总结当前 diff。
```

```text
帮我写 commit message。
```

```text
帮我写 PR 描述。
```

## 真实使用例子

### 用户输入

```text
登录后白屏，帮我修一下。
```

### Skill 应自动执行

1. 判断任务类型为 bug 根因定位。
2. 检查项目是否有 `CLAUDE.md`。
3. 读取 Electron 主进程、preload 文件、渲染进程入口、登录流程相关代码。
4. 搜索错误信息和调用位置。
5. 分析可能原因，例如 preload 没加载、contextBridge 暴露字段不一致、构建路径错误、调用时机过早。
6. 给出根因证据。
7. 提出修复方案。
8. 如果修改涉及核心启动流程，先征求确认。
9. 修改后运行启动检查。
10. 输出修改文件、验证结果和风险点。

### 预期输出

```text
当前发现：

1. 相关文件：
main.ts
preload.ts
LoginPage.tsx
electron-builder 配置

2. 可能根因：
渲染进程调用 window.electronBridge 时，preload 暴露的字段名实际为 window.api，字段不一致导致 undefined。

3. 推荐方案：
统一 preload 暴露字段和渲染进程调用字段，并加一层存在性检查。

4. 风险：
涉及 Electron 主进程和渲染进程通信，需要启动验证。

是否按这个方案修改？
```

## 另一个真实使用例子

### 用户输入

```text
帮我给桌面宠物项目加一个记忆系统。
```

### Skill 应自动执行

1. 判断任务类型为新功能开发。
2. 检查 `CLAUDE.md`。
3. 扫描 Electron 项目结构。
4. 找到聊天模块、状态管理、本地存储、主进程和渲染进程通信方式。
5. 分析当前聊天流程。
6. 分析是否已有 localStorage、SQLite、文件存储或其他数据库。
7. 提出多个方案。
8. 推荐 MVP 方案。
9. 等用户确认后再实现。

### 预期输出

```text
当前项目理解：

1. 聊天入口在 ChatPanel。
2. 消息状态由 useChatStore 管理。
3. 当前没有完整数据库，只保存了部分本地配置。
4. 主进程和渲染进程通过 IPC 通信。

可选方案：

方案一：localStorage
开发最快，适合 Demo，但后续扩展弱。

方案二：SQLite
适合桌面应用，可以保存长期记忆、关系值和聊天摘要。

方案三：向量检索
适合复杂长期记忆，但初期成本较高。

推荐方案：
先用 SQLite 做 MVP，包含 user_memories、pet_relationship、chat_summaries 三类数据。

需要确认：
是否允许新增 SQLite 依赖？
```

## Skill 成功标准

使用这个 Skill 后，应达到以下效果：

1. 用户可以用短指令触发工程流程。
2. Claude Code 会自动判断任务类型。
3. Claude Code 会自动检查 `CLAUDE.md`。
4. Claude Code 会先理解项目，再改代码。
5. Bug 修复会先定位根因。
6. 新功能开发会先给方案。
7. 重构任务会先做风险分析。
8. 前端任务会尽量截图验证。
9. 高风险操作会先确认。
10. 修改完成后会输出验证结果和风险点。
11. 项目经验会逐步沉淀到 `CLAUDE.md`。
12. 团队可以复用同一套工程协作规则。

## 一句话总结

这个 Skill 的作用，是让用户只说简单任务，Claude Code 自动按工程协作流程执行，从项目理解、任务分类、规则读取、方案设计，到代码修改、测试验证和经验沉淀，形成一套稳定的工程开发闭环。
